# Per-service .env files (documented in each service's .env.example)
# This root .env provides variables for docker-compose.yml substitution.
AUTH_SECRET=your-auth-secret
SECRET_ENCRYPTION_KEY=your-encryption-key
